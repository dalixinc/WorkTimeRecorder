I am making WorkTimers for each Task and this doesn't feel efficient.

Should probably be one timer and it could take timer tasks for each. Also, these tasks , even when paused, are still polling
so definite place for optimisation, but let's get an MVP

